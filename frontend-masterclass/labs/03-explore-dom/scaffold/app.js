// Task 1: Find the element with id="title" and read its text content
// Store it in the titleText variable
let titleText = ""; // TODO: replace "" with the actual text content

// Task 2: Count all <p> elements on the page
// Store the count in paragraphCount
let paragraphCount = 0; // TODO: replace 0 with the actual count

// Task 3: Add the class "highlight" to the element with id="featured"
// TODO: find the element and add the class

// Task 4: Create a new <p> with text "Added by JavaScript"
//         and append it to the <main> element
// TODO: create the element, set its text, append it

// --- Do not modify below this line ---
// These exports let the tests read your variables
if (typeof module !== "undefined") {
  module.exports = { titleText, paragraphCount };
}
