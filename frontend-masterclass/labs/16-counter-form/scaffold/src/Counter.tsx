import { useState } from "react";
import "./styles.css";

// TODO: Create a Counter component
// - useState for count, starting at 0
// - Three buttons: increment (+1), decrement (-1), reset (0)
// - Use functional updates: setCount(prev => prev + 1)
// - Display count in a <p>

// TODO: Add a character-counted input
// - useState for text, starting as ""
// - An <input> with value and onChange
// - Display character count DERIVED from text.length (not a separate state)
// - Format: "X / 100 characters"

export function Counter() {
  return <div>{/* Build counter and input here */}</div>;
}
