// TODO: Define a ProfileCardProps interface
// - readonly name: string
// - readonly avatarUrl: string
// - readonly bio: string
// - readonly skills: readonly string[]

// TODO: Create and export a ProfileCard function component
// Destructure props: { name, avatarUrl, bio, skills }
// Return JSX with:
//   - An <img> with src={avatarUrl} and alt={name}
//   - An <h2> with the name
//   - A <p> with the bio
//   - A <ul> with skills mapped to <li> elements (don't forget key!)
