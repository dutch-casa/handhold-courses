import "./styles.css";
// TODO: Import ThemeProvider and useTheme from "./ThemeProvider"

// TODO: Create a ThemeToggle component
// - Uses useTheme() to get theme and toggleTheme
// - Renders a button that calls toggleTheme on click
// - Shows current theme in the button text

// TODO: Create a ThemedCard component
// - Uses useTheme() to get theme
// - Applies className based on theme: "card card-light" or "card card-dark"
// - Renders children inside

// TODO: Wrap App content in <ThemeProvider>
// Render <ThemeToggle /> and a <ThemedCard> with some content

export default function App() {
  return (
    <main>
      {/* Wrap in ThemeProvider, render ThemeToggle and ThemedCard */}
    </main>
  );
}
