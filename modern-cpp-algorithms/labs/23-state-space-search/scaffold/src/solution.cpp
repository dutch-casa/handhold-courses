#include "solution.h"

int open_lock(const std::vector<std::string>& deadends, std::string target) {
    // TODO: BFS from "0000" to target. Each state has 8 neighbors. Avoid deadends.
    return -1;
}

std::vector<int> topo_sort(int n, const std::vector<std::pair<int, int>>& edges) {
    // TODO: Kahn's algorithm. Return empty vector if cycle detected.
    return {};
}
