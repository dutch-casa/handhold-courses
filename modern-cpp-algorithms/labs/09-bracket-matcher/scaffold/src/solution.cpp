#include "solution.h"
#include <stack>

bool is_balanced(std::string_view s) {
    // TODO: Use a stack to check that (), [], {} are all balanced
    return false;
}

int first_unbalanced(std::string_view s) {
    // TODO: Return index of first unbalanced bracket, or -1 if balanced
    return -1;
}
