#include "solution.h"
#include <sstream>
#include <format>

std::map<std::string, int> count_words(std::string_view text) {
    // TODO: Split text into words and count frequencies
    return {};
}

std::string most_frequent(const std::map<std::string, int>& counts) {
    // TODO: Return the word with the highest count
    return "";
}

std::string format_counts(const std::map<std::string, int>& counts) {
    // TODO: Return "word1: N, word2: M, ..." (sorted alphabetically by map order)
    return "";
}
