#include "solution.h"
#include <sstream>

std::map<char, int> char_freq(std::string_view text) {
    // TODO: Count frequency of each character in text
    return {};
}

std::map<std::string, int> word_freq(std::string_view text) {
    // TODO: Split on whitespace and count word frequencies
    return {};
}

std::map<std::string, int> bigram_freq(std::string_view text) {
    // TODO: Count consecutive word pair frequencies, key = "word1 word2"
    return {};
}

std::string most_common_bigram(std::string_view text) {
    // TODO: Return the bigram with the highest frequency
    return "";
}
