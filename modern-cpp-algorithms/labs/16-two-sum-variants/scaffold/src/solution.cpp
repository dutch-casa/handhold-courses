#include "solution.h"
#include <algorithm>

std::pair<int, int> two_sum_sorted(const std::vector<int>& nums, int target) {
    // TODO: Two pointers from both ends. Return 1-indexed pair.
    return {0, 0};
}

std::vector<std::vector<int>> three_sum(std::vector<int> nums) {
    // TODO: Sort, fix one element, two-pointer the rest. Skip duplicates.
    return {};
}

int max_water(const std::vector<int>& heights) {
    // TODO: Two pointers, move the shorter side inward, track max area.
    return 0;
}
