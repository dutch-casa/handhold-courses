#include "solution.h"

int num_islands(std::vector<std::vector<char>>& grid) {
    // TODO: Iterate cells, DFS flood-fill each '1' island, mark visited as '0'
    return 0;
}

int count_components(int n, const std::vector<std::pair<int, int>>& edges) {
    // TODO: Build adjacency list, DFS from each unvisited node, count components
    return 0;
}

bool can_finish(int num_courses, const std::vector<std::pair<int, int>>& prereqs) {
    // TODO: Build directed graph, detect cycles (DFS coloring or Kahn's)
    return false;
}
