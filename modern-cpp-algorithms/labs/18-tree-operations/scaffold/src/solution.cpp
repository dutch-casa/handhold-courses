#include "solution.h"

int max_depth(TreeNode* root) {
    // TODO: Return max depth of binary tree. Null tree has depth 0.
    return 0;
}

TreeNode* invert_tree(TreeNode* root) {
    // TODO: Swap left and right children recursively. Return root.
    return root;
}

bool has_path_sum(TreeNode* root, int target) {
    // TODO: Return true if any root-to-leaf path sums to target.
    return false;
}
