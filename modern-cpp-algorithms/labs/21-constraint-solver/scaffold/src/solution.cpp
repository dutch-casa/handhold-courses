#include "solution.h"

bool solve_sudoku(std::vector<std::vector<int>>& board) {
    // TODO: Find next empty cell (0), try 1-9, check constraints, recurse
    return false;
}

std::vector<std::string> generate_parentheses(int n) {
    // TODO: Backtrack with open/close counts. Add '(' if open < n, ')' if close < open.
    return {};
}
