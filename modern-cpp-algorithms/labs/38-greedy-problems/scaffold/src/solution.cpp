#include "solution.h"

bool can_jump(const std::vector<int>& nums) {
    // TODO: track the farthest reachable index
    return false;
}

int min_jumps(const std::vector<int>& nums) {
    // TODO: greedily jump to the furthest reachable position within the current range
    return 0;
}

int gas_station(const std::vector<int>& gas, const std::vector<int>& cost) {
    // TODO: if total gas >= total cost, greedily find the starting station
    return -1;
}

std::vector<int> partition_labels(const std::string& s) {
    // TODO: precompute last occurrence of each char, then greedily form partitions
    return {};
}
