#include "solution.h"

std::vector<int> spiral_order(const std::vector<std::vector<int>>& matrix) {
    // TODO: track top/bottom/left/right boundaries, traverse each edge and shrink
    return {};
}

void rotate_image(std::vector<std::vector<int>>& matrix) {
    // TODO: transpose, then reverse each row
}

void set_zeroes(std::vector<std::vector<int>>& matrix) {
    // TODO: collect zero positions, then zero out those rows and columns
}

bool search_matrix(const std::vector<std::vector<int>>& matrix, int target) {
    // TODO: binary search treating the matrix as a flat sorted array
    return false;
}
