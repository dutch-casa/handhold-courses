#include "solution.h"

LRUCache::LRUCache(int capacity) : capacity_(capacity) {}

// TODO: look up key in map; if found, splice to front and return value; else return -1
int LRUCache::get(int key) {
    // TODO
    return -1;
}

// TODO: if key exists, update and splice to front;
//       if new and at capacity, evict back; insert at front and update map
void LRUCache::put(int key, int value) {
    // TODO
}
