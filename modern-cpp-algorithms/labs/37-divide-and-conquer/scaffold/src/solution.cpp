#include "solution.h"

std::vector<int> merge_sort(std::vector<int> nums) {
    // TODO: split at midpoint, recurse, merge sorted halves
    return nums;
}

long long count_inversions(std::vector<int> nums) {
    // TODO: count pairs (i,j) where i<j and nums[i]>nums[j]
    // Hint: piggyback on merge sort — count during the merge step
    return 0;
}

int majority_element(const std::vector<int>& nums) {
    // TODO: Boyer-Moore voting algorithm
    return 0;
}
