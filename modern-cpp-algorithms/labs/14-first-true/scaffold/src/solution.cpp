#include "solution.h"

int first_true(const std::vector<int>& arr, std::function<bool(int)> pred) {
    // TODO: Binary search for the first index where pred(arr[i]) is true
    // Return -1 if no element satisfies the predicate
    return -1;
}

int first_bad_version(int n, std::function<bool(int)> is_bad) {
    // TODO: Binary search versions 1..n for the first where is_bad returns true
    return -1;
}

int find_peak(const std::vector<int>& arr) {
    // TODO: Binary search for the peak of a mountain array
    return -1;
}
