#include "solution.h"
#include <sstream>
#include <algorithm>

std::vector<std::string> split_lines(std::string_view text) {
    // TODO: Split text by newline, skipping empty lines
    return {};
}

std::vector<std::string> filter_by_level(const std::vector<std::string>& lines, std::string_view level) {
    // TODO: Keep lines containing the level substring
    return {};
}

std::vector<std::string> take_recent(const std::vector<std::string>& lines, int n) {
    // TODO: Return the last n lines
    return {};
}

int count_level(const std::vector<std::string>& lines, std::string_view level) {
    // TODO: Count lines containing the level substring
    return 0;
}
