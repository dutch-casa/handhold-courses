#include "solution.h"

std::vector<std::pair<int,int>> merge_intervals(std::vector<std::pair<int,int>> intervals) {
    // TODO: sort by start, then greedily merge overlapping intervals
    return {};
}

std::vector<std::pair<int,int>> insert_interval(
    std::vector<std::pair<int,int>> intervals,
    std::pair<int,int> new_interval) {
    // TODO: insert new_interval and merge overlapping intervals
    return {};
}

int min_meeting_rooms(std::vector<std::pair<int,int>> intervals) {
    // TODO: return the minimum rooms needed for all meetings to run
    return 0;
}
