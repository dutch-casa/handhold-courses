#include "solution.h"

int longest_unique_substr(std::string_view s) {
    // TODO: Sliding window — expand right, shrink left on duplicate
    return 0;
}

std::string min_window(std::string_view s, std::string_view t) {
    // TODO: Sliding window — expand until all chars of t covered, shrink to minimize
    return "";
}
