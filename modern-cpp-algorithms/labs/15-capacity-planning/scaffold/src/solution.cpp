#include "solution.h"

bool can_ship(const std::vector<int>& weights, int trucks, int capacity) {
    // TODO: Greedily load trucks left-to-right. Return true if trucks suffice.
    return false;
}

int min_capacity(const std::vector<int>& weights, int trucks) {
    // TODO: Binary search over capacity [max_weight .. sum_weights]
    // using can_ship as the feasibility check
    return 0;
}

int peak_of_mountain(const std::vector<int>& arr) {
    // TODO: Binary search for the peak element
    return -1;
}
