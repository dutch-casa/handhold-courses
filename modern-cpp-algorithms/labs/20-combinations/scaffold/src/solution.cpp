#include "solution.h"

std::vector<std::vector<int>> permutations(std::vector<int> nums) {
    // TODO: Generate all permutations via backtracking
    return {};
}

std::vector<std::vector<int>> combinations(int n, int k) {
    // TODO: Generate all C(n,k) combinations from 1..n
    return {};
}

std::vector<std::vector<int>> subsets(std::vector<int> nums) {
    // TODO: Generate all subsets (power set)
    return {};
}
