#include "solution.h"
#include <algorithm>
#include <cmath>
#include <numeric>

double rolling_mean(std::span<const double> data, int window_size) {
    // TODO: Return the mean of the last window_size elements
    return 0.0;
}

double rolling_median(std::span<const double> data, int window_size) {
    // TODO: Return the median of the last window_size elements
    return 0.0;
}

double rolling_stddev(std::span<const double> data, int window_size) {
    // TODO: Return the population standard deviation of the last window_size elements
    return 0.0;
}
